# build output
/bin/
*.exe
# but DO track the bundled iperf3 shipped inside the Windows binary
!internal/iperf/bundled/iperf3.exe
# local data / databases
*.db
*.db-wal
*.db-shm
data/
# go
/vendor/
# generated Windows resource (manifest/version) — produced by go generate
cmd/netlogger-app/resource.syso
/NetLogger-data/
netlogger.log
netlogger-export-*.json
network.json

# superpowers brainstorm companion (local mockups)
/.superpowers/
